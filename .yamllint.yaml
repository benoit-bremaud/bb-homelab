---
extends: default

rules:
  line-length:
    max: 120
    level: warning
  document-start: disable
  truthy:
    check-keys: false   # GitHub Actions uses bare `on:` keyword
  comments:
    min-spaces-from-content: 1
  comments-indentation: disable

ignore: |
  .git/
  node_modules/
