# Copy to .env and fill in. .env is gitignored.

# Image tag — bump deliberately (test before rolling out).
# UPTIME_KUMA_IMAGE_TAG=2.3.2

# Timezone (defaults to Europe/Paris if unset).
# TZ=Europe/Paris

# No secrets here: the admin account and the Telegram notification
# token are configured in the Uptime Kuma UI on first start, not via
# environment variables.
